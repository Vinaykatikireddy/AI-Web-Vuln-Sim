services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: attack_backend
    ports:
      - "7860:7860"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - SECRET_KEY=${SECRET_KEY}
    restart: "no"